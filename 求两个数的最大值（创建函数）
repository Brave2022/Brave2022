#include <stdio.h>

int get_max(int x,int y)
{
	if (x > y)
		return x;
	else
		return y;
}

int main()
{
	int a = 20, b = 10;
	scanf("%d%d", &a, &b);
	int max =get_max(a,b);
	printf("max = %d", max);
	return 0;
}
