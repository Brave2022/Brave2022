#include <stdio.h>
int factorial(int n)
{
	int ret = 0;
	if (n >= 1)
	{
		return ret = n * factorial(n - 1);
	}
	else
		return 1;
}

int main()
{
	int n = 0;
	scanf("%d", &n);
	int ret = factorial(n);
	printf("ret = %d", ret);
	return 0;
}
